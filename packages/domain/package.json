{
  "name": "@roundtable/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure types and deterministic logic. No IO (AGENTS.md R1).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b && node -e \"const f=require('fs');f.cpSync('src/brackets','dist/brackets',{recursive:true});f.cpSync('src/efficiency','dist/efficiency',{recursive:true})\"",
    "clean": "tsc -b --clean"
  }
}
